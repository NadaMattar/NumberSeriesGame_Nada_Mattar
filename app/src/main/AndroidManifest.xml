<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="na.ma.numper.series">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NumperSeries">

        <activity
            android:name=".views.activities.ShowAllGamesActivity"
            android:configChanges="screenSize|orientation"
            android:exported="true" >
        </activity>

        <activity
            android:name=".views.activities.SettingActivity"
            android:configChanges="screenSize|orientation"
            android:exported="false" >
        </activity>

        <activity
            android:name=".views.activities.RegisterActivity"
            android:configChanges="screenSize|orientation"
            android:exported="true" >
        </activity>

        <activity
            android:name=".views.activities.LoginActivity"
            android:configChanges="screenSize|orientation"
            android:exported="false" >
        </activity>

        <activity
            android:name=".views.activities.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".views.activities.MainActivity"
            android:configChanges="screenSize|orientation"
            android:exported="true" >

        </activity>

        <activity
            android:name=".views.activities.ChangePasswordActivity"
            android:configChanges="screenSize|orientation"
            android:exported="true" >
        </activity>

    </application>

</manifest>